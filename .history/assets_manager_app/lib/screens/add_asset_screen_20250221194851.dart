import 'package:flutter/material.dart';


class AddAssetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Asset'),
      ),
      body: Center(
        child: Text('Add Asset Screen - Coming Soon'),
      ),
    );
  }
} 